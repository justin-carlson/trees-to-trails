{
  "name": "trees-to-trails-placeholder",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-placeholder.js",
    "develop": "npm run build",
    "serve": "npm run build",
    "test": "npm run build"
  }
}
